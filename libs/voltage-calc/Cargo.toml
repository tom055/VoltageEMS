[package]
name = "voltage-calc"
version.workspace = true
edition = "2024"
license.workspace = true
description = "Unified calculation library for VoltageEMS - expression evaluation and built-in functions"

[dependencies]
# Expression evaluation
evalexpr = { workspace = true }

# Async sync primitives (RwLock for state)
tokio = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Pattern matching for function parsing
regex = { workspace = true }

# Time
chrono = { workspace = true }

# Logging
tracing = { workspace = true }

# Real-time database abstraction (for RtdbStateStore)
voltage-rtdb = { path = "../voltage-rtdb", default-features = false }
bytes = { workspace = true }
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

[dev-dependencies]
# No dev dependencies needed

[lints]
workspace = true

